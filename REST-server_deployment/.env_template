# .env_template - example environment variables for docker-compose
#
# Copy this file to `.env` and edit values to match your host environment
# Example: `cp .env_template .env` then edit `.env` before running `docker-compose up`.
#
# Variables in this file are referenced by `docker-compose.yml` using the
# ${VARIABLE_NAME} syntax. docker-compose automatically loads a `.env` file
# from the same directory and substitutes the variables when creating services.

# Container name for the Jupyter service (used as `container_name` in compose)
JUPYTER_CONTAINER_NAME=NAME_OF_YOUR_CONTAINER

# Image tag to use for the Jupyter / REST server container.
# Default points to the pre-built image on Docker Hub maintained by the project.
IMAGE_VERSION=reineros4os/ssd-server:1

# Port mapping for Jupyter Lab/Notebook: <host_port>:<container_port>
# Default maps host port 8012 to container port 8888 (Jupyter default).
JUPYTER_PORT=8012:8888

# Port mapping for Flask REST API: <host_port>:<container_port>
# Default maps host port 5000 to container port 5000 (Flask default).
FLASK_PORT=5000:5000

# Host path for persistent data (host) mounted into the container at
# `/home/jovyan/work/` so notebooks and data persist across container restarts.
# Example (Linux/macOS): /home/username/ssd-data
# Example (Windows with WSL or Docker Desktop): C:\Users\you\ssd-data  (use proper quoting if needed)
PATH_PERSISTENT_VOLUME=/home/username/ssd-data

# Notes on variable linkage:
# - `docker-compose.yml` references these variables, e.g.:
#     image: ${IMAGE_VERSION}
#     container_name: ${JUPYTER_CONTAINER_NAME}
#     volumes:
#       - ${PATH_PERSISTENT_VOLUME}:/home/jovyan/work/
#     ports:
#       - ${JUPYTER_PORT}
#       - ${FLASK_PORT}
# - To change ports or paths, edit `.env` and restart `docker-compose`.